rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /matches/the-matcha-games {
      allow read: if true;
      allow write: if request.auth != null
                   && request.auth.uid == "lDFFGouvEQQwBSoUtOJ7EyQQruM2";
    }
  }
}
